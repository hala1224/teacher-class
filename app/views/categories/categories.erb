<% if flash.has?(:message) %>
      <%= flash[:message] %>
    <% end %>


<h1>Welcome, <%= @teacher.username %>, here are all categories for this Gym:</h1>

<h3>
  <p><% Category.all.each do |category| %>
     <p><a href="/users/<%= category.teacher.slug %>"><%= category.teacher.username %> adding a class! <a href="/categories/<%= category.id %>">"<%= category.content %>"</p>
    <% end %>
  </p> </h3>

      <br>
      <p><a href="/categories/new">Create a New Category</a></p>
        <p><a href="/categories">Home</a></p>
